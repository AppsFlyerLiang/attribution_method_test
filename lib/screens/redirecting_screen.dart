import 'package:flutter/material.dart';

class RedirectingScreen extends StatefulWidget {
  RedirectingScreen({Key key}) : super(key: key);

  @override
  _RedirectingScreenState createState() => _RedirectingScreenState();
}

class _RedirectingScreenState extends State<RedirectingScreen> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}